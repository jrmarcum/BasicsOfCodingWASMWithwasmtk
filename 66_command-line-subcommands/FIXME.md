## Compilation Error

**Error:** `undefined local variable "$s"` in WAT  
**Root cause:** String accumulator scoping issue in a `string[]` formatting helper — same pattern as 40_sorting  
**Fix needed:** Same fix as 40_sorting
